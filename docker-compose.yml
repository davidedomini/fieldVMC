services:
  optimizing-simulations:
    image: angelacortecchia/field-vmc-simulation:1.12.1
    build:
      context: .
      dockerfile: ./docker/cuttingSimulations/Dockerfile
    volumes:
      - angela.cortecchia-volume:/data
    environment:
      - GRADLE_TASK=runClassicVMCBatch
      - GRADLE_TASK=runFixedLeaderOptimizer

volumes:
  angela.cortecchia-volume:
    external: true